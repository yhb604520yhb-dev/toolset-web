# Cloudflare Pages 部署指南

## ⚠️ Windows 系统说明

**重要**：`@cloudflare/next-on-pages` 在 Windows 系统上本地构建可能不可靠。**推荐直接通过 Git 集成在 Cloudflare Pages 上构建**（构建会在 Linux 环境中进行，不会有兼容性问题）。

如果你需要本地测试构建，建议使用：
- **WSL (Windows Subsystem for Linux)**
- 或者在 Linux/Mac 环境中构建

## 前置步骤

### 1. 安装依赖
```bash
npm install --save-dev @cloudflare/next-on-pages
```
> 注意：在 Windows 上本地测试构建可能会失败，但不影响在 Cloudflare Pages 上部署

### 2. 本地测试构建（可选）
```bash
# 标准 Next.js 构建（本地开发）
npm run build

# Cloudflare Pages 构建（Windows 上可能失败，但不影响部署）
# 如果失败，可以跳过此步骤，直接部署到 Cloudflare Pages
npm run pages:build

# 本地预览 Cloudflare 构建结果（需要先成功运行 pages:build）
npm run preview:cloudflare
```

## Cloudflare Pages 部署配置

### 方式A：通过 Git 集成（推荐，Windows 用户首选）

1. **连接仓库**
   - 在 Cloudflare Pages 控制台点击 "Create a project"
   - 选择你的 Git 仓库（GitHub/GitLab/Bitbucket）

2. **构建设置**
   - **Framework preset**: Next.js（或 Custom）
   - **Build command**: `npm install && npm run build && npx @cloudflare/next-on-pages`
   - **Build output directory**: `.vercel/output/static`
   - **Root directory**: `/`（项目根目录）
   - **Node.js version**: `20.x`（或最新稳定版）

3. **环境变量**
   在 Cloudflare Pages 控制台添加以下环境变量：
   - `NEXT_PUBLIC_SORA2_API_URL` = `https://ai.604520.top`
   - `NEXT_PUBLIC_SORA2_API_KEY` = （你的 API 密钥）
   - `NEXT_PUBLIC_SORA2_TIMEOUT` = `60000`
   - `NEXT_PUBLIC_SORA2_DEFAULT_MODEL` = `sora-2`
   - `NEXT_PUBLIC_SORA2_ENDPOINT_TYPE` = `openai-official`

### 方式B：通过 Wrangler CLI

```bash
# 安装 Wrangler（如果还没安装）
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 构建并部署
npm run deploy:cloudflare
wrangler pages deploy .vercel/output/static --project-name=toolset-web
```

## 注意事项

1. **Windows 兼容性**：在 Windows 上，`@cloudflare/next-on-pages` 本地构建可能失败，但**不影响在 Cloudflare Pages 上的部署**（构建会在 Linux 环境中进行）
2. **环境变量**：所有以 `NEXT_PUBLIC_` 开头的变量会在构建时内嵌到客户端代码中
3. **API Routes**：已配置为 Edge Runtime，兼容 Cloudflare Workers
4. **本地开发**：修改后本地开发环境（`npm run dev`）仍可正常使用，不受影响
5. **构建大小**：注意 Cloudflare Pages 的构建大小限制（通常为 100MB）
6. **标准构建测试**：即使 `pages:build` 在 Windows 上失败，只要 `npm run build` 成功，代码就可以在 Cloudflare Pages 上正常部署

## 验证部署

部署完成后，检查：
- ✅ 页面正常加载
- ✅ API Routes 正常工作（`/api/sora/characters`, `/api/upload-image`）
- ✅ 视频生成功能正常
- ✅ 环境变量正确加载

