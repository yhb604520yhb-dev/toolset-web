# 🔄 如何触发新的 Cloudflare Pages 构建

## 📋 当前情况
- 所有构建都失败（红色警告图标）
- Cloudflare Dashboard 没有"重新部署"按钮
- 需要手动触发新的构建

## ✅ 方法一：推送空提交触发构建（最简单）

在终端执行：

```bash
git commit --allow-empty -m "触发重新部署"
git push origin new-branch-name
```

这会触发 Cloudflare Pages 自动开始新的构建。

## ✅ 方法二：修改构建命令后推送代码

1. **确认构建命令已正确配置**：
   - 进入 Cloudflare Dashboard
   - **设置** → **构建命令**
   - 确保命令为：
   ```
   npm install --legacy-peer-deps && npm run build && npx @cloudflare/next-on-pages --disable-worker-minification
   ```

2. **推送代码触发**：
   ```bash
   git push origin new-branch-name
   ```

## ✅ 方法三：对代码做小改动

对任何文件做一个小改动（比如添加一行注释），然后：

```bash
git add .
git commit -m "修复构建配置"
git push origin new-branch-name
```

## ⚠️ 重要：先查看失败原因

**在触发新构建之前**，请先：
1. 点击最新的失败构建（11 分钟前的 `40a0489`）
2. 点击 **"查看构建"**
3. 查看详细错误日志
4. 如果是 `async_hooks` 错误，确保构建命令中有 `--disable-worker-minification`

## 📝 推荐步骤

1. ✅ 查看最新失败构建的日志
2. ✅ 确认 Cloudflare Dashboard 中的构建命令正确
3. ✅ 执行以下命令触发新构建：
   ```bash
   git commit --allow-empty -m "重新部署"
   git push origin new-branch-name
   ```
4. ✅ 等待 2-5 分钟，然后查看新的构建状态

