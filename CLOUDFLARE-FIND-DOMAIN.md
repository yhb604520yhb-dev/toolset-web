# 🔍 如何找到 Cloudflare Pages 的默认子域名

## 📍 方法一：在"部署"标签页查看（最简单）

### 步骤：

1. **进入"部署"标签页**
   - 点击顶部的 **"部署"** (Deployment) 标签

2. **查看部署详情**
   - 找到带有"最新"绿色标签的部署
   - 点击这个部署条目，或者
   - 在部署条目的右侧，通常会显示访问 URL

3. **或查看"查看构建历史记录"**
   - 点击 **"查看构建历史记录"** (View build history)
   - 打开最新的成功构建
   - 在构建详情中应该能看到访问 URL

## 📍 方法二：在"概述"标签页查看

### 步骤：

1. **进入"概述"标签页**
   - 点击顶部的 **"概述"** (Overview) 标签

2. **查看"域和路由"面板**
   - 找到 **"域和路由"** (Domain and Routes) 面板
   - 点击右侧的箭头图标展开
   - 应该能看到类似这样的 URL：
     - `toolset-web.pages.dev`
     - `toolset-web.yhb604520yhb.workers.dev`
     - 或预览 URL：`*-toolset-web.pages.dev`

3. **如果显示"已禁用"**
   - 点击域名旁边的按钮启用
   - 或在"设置"页面启用

## 📍 方法三：在"设置"标签页查看

### 步骤：

1. **进入"设置"标签页**
   - 点击顶部的 **"设置"** (Settings) 标签

2. **查找域名设置**
   - 向下滚动，查找 **"自定义域"** (Custom domains) 部分
   - 或 **"域和路由"** (Domains and Routes) 部分
   - 应该能看到所有的域名列表，包括默认的 Pages 域名

## 📍 方法四：通过项目名称推断

### Cloudflare Pages 默认域名格式：

通常格式为：`项目名称.pages.dev`

根据你的项目名称 `toolset-web`，默认域名应该是：
- **主域名**：`toolset-web.pages.dev`
- **或**：`toolset-web.yhb604520yhb.pages.dev`（如果带账户前缀）

## 📍 方法五：直接访问测试

如果找不到，可以直接尝试访问：

1. **标准格式**：
   ```
   https://toolset-web.pages.dev
   ```

2. **带账户前缀**：
   ```
   https://toolset-web.yhb604520yhb.pages.dev
   ```

3. **workers.dev 格式**（如果项目也配置为 Worker）：
   ```
   https://toolset-web.yhb604520yhb.workers.dev
   ```

## 📝 如果所有域名都显示"已禁用"

### 启用默认 Pages 域名：

1. **在"设置"标签页**：
   - 进入 **"设置"** 标签
   - 找到 **"自定义域"** 或 **"域和路由"** 部分
   - 找到 `*.pages.dev` 或 `workers.dev` 域名
   - 点击 **"启用"** 按钮

2. **或在"概述"标签页**：
   - 在"域和路由"面板中
   - 点击域名旁边的开关或按钮启用

## 🔍 如果仍然找不到

### 检查项目类型：

1. **确认是 Pages 项目**
   - 在"概述"标签页查看项目类型
   - 应该显示为 "Pages" 项目

2. **查看项目信息**
   - 在"概述"标签页的顶部
   - 查看项目名称和状态

3. **联系支持或检查文档**
   - 如果以上方法都找不到，可能是项目配置问题
   - 可以在 Cloudflare Dashboard 的帮助中心搜索

## ✅ 快速验证

即使找不到域名设置，你也可以：

1. **访问部署 URL**
   - 从"部署"标签页的部署详情中获取

2. **检查部署状态**
   - 确保部署成功（有绿色"最新"标签）
   - 部署成功后，默认域名应该自动可用

3. **使用预览 URL**
   - 每次部署都会生成一个唯一的预览 URL
   - 格式类似：`xxxxx-toolset-web.pages.dev`

