# 🔄 如何重新部署和查看构建失败原因

## 📋 查看构建失败日志

### 步骤 1：查看最新的失败构建
1. 在构建历史记录表中，找到最新的失败构建（11 分钟前的 `40a0489`）
2. 点击该行的 **"查看构建"** 按钮
3. 查看构建日志，找到错误信息

### 步骤 2：识别错误类型
常见的错误：
- `Could not resolve "async_hooks"` → 构建命令缺少 `--disable-worker-minification`
- `npm error` → 依赖安装问题
- `Build failed` → 其他构建错误

## 🔄 触发重新部署的方法

### 方法一：推送新的代码（推荐）
这是触发新构建最简单的方法：

```bash
# 做一个小的修改（比如更新 README 或添加注释）
# 然后提交并推送
git commit --allow-empty -m "触发重新部署"
git push origin new-branch-name
```

或者，如果你修改了构建命令配置，直接推送：

```bash
git push origin new-branch-name
```

### 方法二：在 Cloudflare Dashboard 中重新部署
1. 找到**成功**的历史构建（如果有的话）
2. 点击该构建记录右侧的 **"..."** 菜单
3. 选择 **"重新部署"** 或 **"重新部署到生产"**

### 方法三：通过 API 触发（高级）
如果 Cloudflare Dashboard 没有重新部署按钮，可以通过 API 触发。

## 🔧 修复构建命令后重新部署

### 确认构建命令正确
1. 进入 Cloudflare Dashboard
2. **Workers & Pages** → **toolset-web** → **设置**
3. 检查 **"构建命令"** 是否为：
   ```
   npm install --legacy-peer-deps && npm run build && npx @cloudflare/next-on-pages --disable-worker-minification
   ```
4. 确保 `--disable-worker-minification` 前面有 `--`（两个短横线）

### 触发新构建
修改构建命令后，有两种方式触发：
1. **自动触发**：推送新代码到 GitHub（任何小改动都可以）
2. **手动等待**：下次推送代码时会自动使用新命令

## 📝 快速重新部署步骤

1. **查看最新的失败日志**：
   - 点击 11 分钟前的构建记录的 **"查看构建"**
   - 复制错误信息

2. **确认构建命令正确**：
   - 设置 → 构建命令 → 检查是否有 `--disable-worker-minification`

3. **触发新构建**：
   ```bash
   git commit --allow-empty -m "重新部署 - 修复构建命令"
   git push origin new-branch-name
   ```

4. **等待并检查**：
   - 等待 2-5 分钟
   - 刷新 Cloudflare Dashboard
   - 查看新的构建状态

## ⚠️ 如果所有构建都失败

如果所有构建都显示失败（红色警告），说明：
- 构建命令可能有问题
- 或者代码/配置有问题

**立即检查**：
1. 点击最新的失败构建（11 分钟前的）
2. 查看详细日志
3. 复制错误信息
4. 根据错误信息修复问题

