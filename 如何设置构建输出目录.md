# 📍 如何设置 Cloudflare Pages 构建输出目录

## 🔍 在哪里找到构建输出目录设置

### 方法一：在构建配置页面

1. **进入 Cloudflare Dashboard**
   - 访问：https://dash.cloudflare.com
   - 导航到：**Workers & Pages** → **toolset-web**

2. **进入设置页面**
   - 点击顶部的 **"设置"** 标签页
   - 或者点击右上角的设置图标

3. **找到构建配置部分**
   - 在设置页面中，找到 **"构建和部署"** (Build & Deploy) 或 **"构建配置"** (Build Configuration) 部分
   - 这个部分应该包含：
     - 构建命令 (Build Command)
     - **构建输出目录** (Build Output Directory) ← **这里！**
     - 根目录 (Root Directory)

### 方法二：在项目设置中

1. **进入项目设置**
   - Workers & Pages → toolset-web → **设置**

2. **查找构建配置**
   - 向下滚动，找到 **"构建配置"** 部分
   - 点击右侧的 **铅笔图标** (编辑按钮)

3. **编辑构建输出目录**
   - 在打开的编辑对话框中
   - 找到 **"构建输出目录"** 或 **"Output Directory"** 字段

## ✅ 需要设置的值

将构建输出目录设置为：

```
.opennext
```

**重要**：
- ⚠️ 从原来的 `.vercel/output/static` 改为 `.opennext`
- ✅ 这是 OpenNext 的标准输出目录

## 📝 完整配置检查清单

确保以下设置都已正确配置：

- [x] **构建命令**：`npm install --legacy-peer-deps && npm run build && npx @opennextjs/cloudflare build` ✅
- [ ] **构建输出目录**：`.opennext` ⚠️ **需要设置**
- [x] **根目录**：`/` ✅
- [x] **Node.js 版本**：`20.x` 或 `22.x` ✅

## 🔧 如果找不到构建输出目录字段

如果设置页面中没有显示"构建输出目录"字段，可能的原因：

1. **需要先点击编辑按钮**
   - 在构建配置部分，点击右侧的 **铅笔图标** 或 **"编辑"** 按钮
   - 这会在弹出窗口或展开区域显示所有字段

2. **可能需要保存当前更改**
   - 先保存构建命令的更改
   - 然后再次进入编辑模式

3. **字段名称可能不同**
   - 可能显示为 "Output Directory"
   - 或 "Build Output Directory"
   - 或 "发布目录"

## ⚠️ 重要提示

如果不设置构建输出目录为 `.opennext`：
- ❌ Cloudflare Pages 可能找不到构建产物
- ❌ 部署会失败或显示错误
- ✅ 设置为 `.opennext` 后，OpenNext 构建的输出才能被正确部署

